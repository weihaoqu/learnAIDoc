---
type: cover
palette: earth
rendering: flat-vector
---

# Content Context
Article title: Claude Code Git Worktree: Parallel Agents Without Conflicts
Content summary: Claude Code's native Git worktree support lets agents run in parallel without conflicts. Each agent gets its own isolated working copy of the repo, enabling true parallel development, safe experimentation, and faster batch operations.
Keywords: git, worktree, parallel, isolation, branches, agents, concurrent, workflow

# Visual Design
Cover theme: parallel branching paths
Type: conceptual
Palette: earth
Rendering: flat-vector
Font: clean
Text level: title-only
Mood: balanced
Aspect ratio: 16:9
Language: en

# Text Elements
Title: "Claude Code Git Worktree"

# Mood Application
Use medium contrast, normal saturation, balanced visual weight

# Font Application
Use clean geometric sans-serif typography. Modern, minimal letterforms.

# Composition
Type composition:
- Abstract shapes representing core concepts, information hierarchy, clean zones

Visual composition:
- Main visual: A tree trunk splitting into 3-4 parallel branches, each branch holding its own miniature terminal/code window — visually representing independent worktrees. The branches grow from a shared root (the git repo) but spread out into their own space. Small robot/agent icons sit on each branch working independently.
- Layout: Tree trunk at bottom-center, branches fanning upward and outward, each with its own workspace, generous spacing between branches
- Decorative: Git branch icons, small file icons on each branch, dotted lines showing isolation boundaries between worktrees

Color scheme: Forest green (#2D5F2D) primary, warm brown (#8B6F47) trunk, sage green (#87A878) branches, cream (#F5F0E0) background elements
Rendering notes: Clean geometric flat-vector shapes, nature-inspired but stylized, clear visual separation between parallel branches
Type notes: Abstract tree metaphor for parallel isolated workspaces
Palette notes: Earth palette reinforcing the organic tree/branch metaphor
