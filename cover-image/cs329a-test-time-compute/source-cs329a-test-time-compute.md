# CS329A Part 2: Test-Time Compute Scaling

More inference compute creates options, not guarantees. A fixed language model can generate many candidates, revise paths, and search over possible answers, but reliability depends on budget allocation, verification, and stopping.

The cover should make the system relationship visible: a locked model produces a branching field of candidates, a finite compute meter routes effort, and a narrow verification gate returns one answer.
